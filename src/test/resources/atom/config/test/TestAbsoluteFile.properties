# test a file property

$class=atom.examples.ExampleComponent3
$scope=global

# a file guaranteed to exist on all Windows platforms
windowsFile=C:/Windows/win.ini

# a file guaranteed to exist on all Linux platforms
linuxFile=/bin/bash
